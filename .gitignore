# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Environments / tooling
.venv/
.pytest_cache/
.ruff_cache/
/.airflow-home/
/.idea/

# Local scratch. *.parquet is anchored to the repo root so a deliberate test fixture under
# tests/ is not silently ignored.
/tmp/
/*.parquet

# Backups left behind if scripts/build-breeze-wheel.sh is interrupted mid-build
*.bak

# Private working notes (design narrative lives in docs/)
/.airflow-sql-as-dag/
